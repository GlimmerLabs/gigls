#lang racket

(provide (all-defined-out))

(define ADD 0)
(define SUBTRACT 1)
(define REPLACE 2)
(define INTERSECT 3)

(define CHANNEL-OP-ADD 0)
(define CHANNEL-OP-SUBTRACT 1)
(define CHANNEL-OP-REPLACE 2)
(define CHANNEL-OP-INTERSECT 3)